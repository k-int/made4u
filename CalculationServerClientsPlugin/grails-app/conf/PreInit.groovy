/**
 * Config class to make sure that connections don't time out for incoming connections
 * Taken from http://jira.codehaus.org/browse/GMOD-50
 */

grails.compiler.dependencies = { fileset(file:'grails-app/src/java/cxf.xml') }

